# An example match field TLV
# See section A.2.3.2 Flow Match Field Structures of the 1.3 Spec.
#
# Basic :: UDP_DST (no mask)

# Class (u16) : Basic = 0x8000
80 00

# Field + hasMask Bit (u8) : UDP_DST(16<<1) (no mask) = 0x20
20

# Length of payload excluding header (u8) = 2
02

## Payload
# UDP destination port (u16) = 78
00 4e
