# An example match field TLV
# See section A.2.3.2 Flow Match Field Structures of the 1.3 Spec.
#
# Basic :: PBB_ISID (with mask)

# Class (u16) : Basic = 0x8000
80 00

# Field + hasMask Bit (u8) : PBB_ISID(37<<1) (with mask) = 0x4b
4b

# Length of payload excluding header (u8) = 6
06

## Payload
# I-SID of PBB service instance tag (u24) = 0x987654
98 76 54

# Mask
0f 0f 0f
