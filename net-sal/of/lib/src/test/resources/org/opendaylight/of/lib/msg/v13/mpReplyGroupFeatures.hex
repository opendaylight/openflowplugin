### OpenFlow 1.3 MULTIPART_REPLY message : Type GROUP_FEATURES
#
# openflow version (u8) = 1.3 (0x4)
#<0>#
04

# message type (u8) = OFPT_MULTIPART_REPLY(19)
13

# message length (u16) = 56
00 38

# transaction id (u32) = 0
00 00 00 00

### Message Specific Payload
## Multipart Reply Header
# Multipart-Type (u16) = GROUP_FEATURES (8)
#<8>#
00 08

# Flags (u16) : none
00 00

# padding (u8 x 4)
00 00 00 00

## Multipart Request Body
# Supported group types bitmap (u32) = ALL, SELECT, INDIRECT, FF (15)
#<16>#
00 00 00 0f

# Capabilities bitmap (u32) = SELECT_WEIGHT, CHAINING (5)
00 00 00 05

## Maximum number of groups for each type (4 x u32)
# one entry per group type in group type code order (supported or not)
#<24>#
00 00 00 01
00 00 00 02
00 00 00 03
00 00 00 04

## Supported actions for each group type - bitmap (4 x u32)
## one entry per group type in group type code order (supported or not)
# Type All - OUTPUT, SET_MPLS_TTL, DEC_MPLS_TTL, GROUP = 0x0418001
#<40>#
00 41 80 01

# Type SELECT - OUTPUT, PUSH_VLAN, POP_VLAN, SET_QUEUE, SET_FIELD = 0x02260001
02 26 00 01

# Type INDIRECT - COPY_TTL_IN, GROUP, DEC_NW_TTL, PUSH_PBB, POP_PBB = 0x0d401000
0d 40 10 00

# Group Type FF - all action types - 0x0fff9801
0f ff 98 01

#<56>#
#### and we are done ####

