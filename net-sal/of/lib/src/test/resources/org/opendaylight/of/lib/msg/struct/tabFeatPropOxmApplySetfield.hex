# An example table feature property :: OXM Apply Setfield

#----------------------------------
## Table feature property - Header

# property type (u16) = APPLY_SETFIELD (14)
#<0>#
00 0e

# length (u16) = 12
00 0c

#----------------------------------
## Table feature property Payload
# fields that can be set (via APPLY_ACTIONS)

# fields are ordered corresponding to OxmBasicFieldType enum so that unit
# tests can reproduce the exact same structure.

# list of OXM types : (length - 4) bytes containing OXM ids
#  32-bit OXM headers or 64-bit OXM headers for experimenter OXM fields
## OXM-basic class, IPV4_SRC(11<<1) (!mask),  payload length
#<4>#
   80 00            16                        00
## OXM-basic class, IPV4_DST(12<<1) (!mask),  payload length
#<8>#
   80 00            18                        00
## padding of 4 bytes to bring structure up to 16 bytes
#<12>#
00 00 00 00

#<16>#
## And we are done ##
