# Example match structure
# See section A.2.3 Flow Match Structures of the 1.3 Spec.

# Match field pre-requisite test
# IP_ECN pre-requisite: ETH_TYPE = IPv4 or IPv6
# Validation should FAIL

## Match Header
# Type (u16) = OXM (0x1)
00 01
# Length (u16) = 20 (excludes padding)
00 14

## Match Field
# Basic, ETH_TYPE(5<<1),   len
  8000   0a                02
# IPv4
  0800

## Match Field
# Basic, IP_ECN(9<<1),   len
  8000   12              01
# code point (u2)
  02

## Match Field (DUPLICATED)
# Basic, IP_ECN(9<<1),   len
  8000   12              01
# code point (u2)
  03

## Zero-Filled Padding to bring total structure
##   length to multiple of 8.
## Match struct length is 20; add 4 = 16
00 00 00 00
